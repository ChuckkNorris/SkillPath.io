import { StaggerGapDirective } from './stagger-gap.directive';

describe('StaggerGapDirective', () => {
  it('should create an instance', () => {
    const directive = new StaggerGapDirective();
    expect(directive).toBeTruthy();
  });
});
