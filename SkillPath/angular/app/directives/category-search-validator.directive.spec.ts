import { CategorySearchValidatorDirective } from './category-search-validator.directive';

describe('CategorySearchValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CategorySearchValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
